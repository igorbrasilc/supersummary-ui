.PHONY: install build dev test storybook lint clean changeset version publish

# Install dependencies
install:
	pnpm install

# Build all packages
build:
	pnpm build

# Run development servers for all packages
dev:
	pnpm dev

# Run tests across all packages
test:
	pnpm test

# Run linting
lint:
	pnpm -r lint